import { SubscriptionFeature } from "@/core";
import { IGenericRepository } from "./generic-repository.abstract";

export abstract class ISubscriptionFeatureRepository extends IGenericRepository<SubscriptionFeature> {}
