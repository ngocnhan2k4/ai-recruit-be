/** Maximum number of questions per exam session (startExam). */
export const EXAM_MAX_QUESTIONS = 10;

/** Minimum exam score required to upsert the skill into user_skills. */
export const EXAM_USER_SKILL_MIN_SCORE = 70;

/** user_skills.source value when the skill is added from a passed exam. */
export const USER_SKILL_SOURCE_EXAM = "exam";
